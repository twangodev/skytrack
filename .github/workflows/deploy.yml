name: Build and Deploy

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '0 */3 * * *'

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
      - name: Install Dependencies
        run: bun install --frozen-lockfile
      - name: Fetch Market Data
        run: bun run fetch --force
      - name: Commit Market History
        if: github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/history
          git diff --cached --quiet || (git commit -m "chore(data): update market history" && git push)
      # data-only cron runs reuse the code already validated on push
      - name: Test
        if: github.event_name != 'schedule'
        run: bun run test
      - name: Check
        if: github.event_name != 'schedule'
        run: bun run check
      # cron schedules are disabled after 60 days without repo activity;
      # re-enable explicitly so a long Hypixel outage can't freeze the site
      - name: Keepalive
        if: github.event_name == 'schedule'
        uses: gautamkrishnar/keepalive-workflow@v2
      - name: Build
        run: bun run build
      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
